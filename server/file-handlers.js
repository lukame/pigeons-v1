/**
 * Created by lukame on 2/20/14.
 */



/*
var payslipsHandler = {
    "default1": function (options) {
        console.log('I am handling default1: ' + options.fileRecord.filename);
        console.log(options.destination().fileData );
        return {
            blob: options.blob,
            fileRecord: options.fileRecord
        };
    }
}

PayslipsFS.fileHandlers(payslipsHandler);
*/